@model Buffet.ViewModels.Buffet.SituacaoConvidado.SituacaoConvidadoViewModel;

@{
    ViewData["Title"] = "Lista Situação do convidado";
}

<div class="container">
    <div class="row">
        <div class="col">
            <div class="card">
                <p class="card-header">Cadastro Situação do convidado</p>
                @if (@Model.MensagemSucesso != null)
                {
                    <div class="alert alert-success" role="alert">
                        @Model.MensagemSucesso
                    </div>
                }
                @if (@Model.MensagemErro != null)
                {
                    <div class="alert alert-danger" role="alert">
                        @Model.MensagemErro
                    </div>
                }
                <div class="card-body">
                    <form method="post" asp-controller="SituacaoConvidado" asp-action="Create">
                        <div class="form-group">
                            <label for="descricao">descricao</label>
                            <input class="form-control" type="text" name="descricao" id="descricao" />
                        </div>
                        <input class="btn btn-primary" type="submit" value="Cadastrar" />
                    </form>
                </div>
            </div>
        </div>
    </div>
    <hr />
    <h4>Situacao do Convidado</h4>
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Descricao</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>

            @if (@Model.SituacaoConvidados.Any())
            {
                var i = 0;
                @foreach (var situacaoConvidado in @Model.SituacaoConvidados)
                {
                    i++;
                    <tr>
                        <td>@i</td>
                        <td>@situacaoConvidado.Descricao</td>
                        <td>
                            <a href="/SituacaoConvidado/Edit/@situacaoConvidado.Id">Mostrar/Editar</a> |
                            <a href="/SituacaoConvidado/Delete/@situacaoConvidado.Id">Deletar</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <th class="text-center" colspan="3">Não há Situação de convidados cadastrados</th>
                </tr>
            }
        </tbody>
    </table>
</div>