@model Buffet.ViewModels.Buffet.SituacaoConvidado.EditarSituacaoConvidadoViewModel;

@{
    ViewData["Title"] = "Lista de Tipos situação do convidado";
}

<div class="container">
    <div class="row">
        <div class="col">
            <div class="card">
                <p class="card-header">Editar Situação do Convidado @Model.Descricao </p>
                <div class="card-body">
                    <form action="/SituacaoConvidado/Edit/@Model.Id" method="post">
                        <div class="form-group">
                            <label for="codigo">Código da situação do convidado</label>
                            <input class="form-control" type="text" id="codigo" value="@Model.Id" disabled />
                        </div>
                        <div class="form-group">
                            <label for="descricao">descricao</label>
                            <input class="form-control" type="text" name="descricao" id="descricao" value="@Model.Descricao" />
                        </div>

                        <div class="form-group">
                            <input class="btn btn-success" type="submit" value="Editar" />
                            <a class="btn btn-primary" href="/SituacaoConvidado/SituacaoConvidados" >Voltar</a>
                        </div>
                        
                    </form>
                </div>
            </div>
        </div>
    </div>
    
</div>