@model Buffet.ViewModels.Buffet.SituacaoConvidado.DeleteSituacaoConvidadoViewModel;

@{
    ViewData["Title"] = "Deletar Situação do Convidado";
}

<div class="container">
    <div class="row">
        <div class="col">
            <div class="card">
                <p class="card-header">Deletar Situação do Convidado @Model.Descricao</p>
                <div class="card-body">
                    <div class="alert alert-danger" role="alert">
                        <strong>Atenção:</strong> Essa operação não pode ser desfeita!
                    </div>
                    <form action="/SituacaoConvidado/DeleteIt/@Model.Id" method="post">
                        <div class="form-group">
                            <label for="codigo">Código da situação do convidado</label>
                            <input class="form-control" type="text" id="codigo" value="@Model.Id" readonly />
                        </div>
                        <div class="form-group">
                            <label for="descricao">descricao</label>
                            <input class="form-control" type="text" name="descricao" id="descricao" value="@Model.Descricao" readonly />
                        </div>
                        <div class="form-group">
                            <input class="btn btn-success" type="submit" value="Deletar" />
                            <a class="btn btn-primary" href="/SituacaoConvidado/SituacaoConvidados">Voltar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
</div>